import React from "react";

const Navbar = () => {
  return (
    <div>
      <button class="lg:dark:hover:bg-white ...">
          dark mode
      </button>
    </div>
  );
};

export default Navbar;
